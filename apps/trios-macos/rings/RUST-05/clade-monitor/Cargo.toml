[package]
name = "clade-monitor"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

description = "TRIOS Clade Cron Monitor - background health + seal + budget checks"

[dependencies]
chrono = "0.4"
libc = "0.2"
reqwest = { version = "0.12", features = ["blocking"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
signal-hook = "0.3"
trios-config = { path = "../../RUST-00/trios-config" }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
