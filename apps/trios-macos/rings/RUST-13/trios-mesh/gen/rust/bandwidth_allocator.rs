// Generated from .t27 spec
// DO NOT EDIT — generated by t27c

pub const MAX_FLOWS: u32 = 8;

pub const TOTAL_BANDWIDTH: u32 = 1000;

pub const MIN_BANDWIDTH: u32 = 10;

pub const MAX_BANDWIDTH: u32 = 500;

pub fn create_flow_requirement(flow_id: u32, priority: u32, min_bw: u32, current_bw: u32) -> u32 {
    return (((((flow_id & 0xFF) << 24) | ((priority & 0x3) << 22)) | ((min_bw & 0x3FF) << 12)) | (current_bw & 0xFFF));
}

pub fn get_flow_id(req: u32) -> u32 {
    return ((req >> 24) & 0xFF);
}

pub fn get_flow_priority(req: u32) -> u32 {
    return ((req >> 22) & 0x3);
}

pub fn get_min_bandwidth(req: u32) -> u32 {
    return ((req >> 12) & 0x3FF);
}

pub fn get_current_bandwidth(req: u32) -> u32 {
    return (req & 0xFFF);
}

pub fn create_allocation_state(allocated: u32, pending: u32, fair_share: u32, last_update: u32) -> u32 {
    return (((((allocated & 0x7FF) << 21) | ((pending & 0xFF) << 13)) | ((fair_share & 0x1FF) << 4)) | (last_update & 0xF));
}

pub fn get_allocated_bw(state: u32) -> u32 {
    return ((state >> 21) & 0x7FF);
}

pub fn get_pending_requests(state: u32) -> u32 {
    return ((state >> 13) & 0xFF);
}

pub fn get_fair_share(state: u32) -> u32 {
    return ((state >> 4) & 0x1FF);
}

pub fn get_last_update(state: u32) -> u32 {
    return (state & 0xF);
}

pub fn create_flow_array(f0: u32, f1: u32, f2: u32, f3: u32, f4: u32, f5: u32, f6: u32, f7: u32) -> u64 {
    return ((((((((() << 56) | (() << 48)) | (() << 40)) | (() << 32)) | (() << 24)) | (() << 16)) | (() << 8)) | ());
}

pub fn get_flow_req(array: u64, index: u32) -> u32 {
    if (index == 0) {
        return ();
    }
    if (index == 1) {
        return ();
    }
    if (index == 2) {
        return ();
    }
    if (index == 3) {
        return ();
    }
    if (index == 4) {
        return ();
    }
    if (index == 5) {
        return ();
    }
    if (index == 6) {
        return ();
    }
    return ();
}

pub fn calculate_fair_share(total_bw: u32, flow_count: u32) -> u32 {
    if (flow_count == 0) {
        return 0;
    }
    return (total_bw / flow_count);
}

pub fn allocate_bandwidth(state: u32, flow_req: u32, available_bw: u32) -> u32 {
    let;
    let;
    let;
    allocated = get_allocated_bw(state);
    let;
    allocation = 0;
    if (priority == 0) {
        allocation = (min_bw + ((available_bw * 7) / 10));
    } else {
        if (priority == 1) {
            allocation = calculate_fair_share(available_bw, 2);
        } else {
            allocation = min_bw;
        }
    }
    if (allocation > available_bw) {
        allocation = available_bw;
    }
    if (allocation > MAX_BANDWIDTH) {
        allocation = MAX_BANDWIDTH;
    }
    if (allocation < min_bw) {
        allocation = min_bw;
    }
    let;
    new_allocated = (allocated + allocation);
    let;
    pending = get_pending_requests(state);
    let;
    fair_share = get_fair_share(state);
    let;
    update = get_last_update(state);
    return create_allocation_state(new_allocated, pending, fair_share, update);
}

pub fn needs_more_bandwidth(flow_req: u32) -> bool {
    let;
    current = get_current_bandwidth(flow_req);
    let;
    min_bw = get_min_bandwidth(flow_req);
    return (current < min_bw);
}

pub fn update_flow_bandwidth(flow_req: u32, new_bw: u32) -> u32 {
    let;
    flow_id = get_flow_id(flow_req);
    let;
    priority = get_flow_priority(flow_req);
    let;
    min_bw = get_min_bandwidth(flow_req);
    if (new_bw < min_bw) {
        new_bw = min_bw;
    }
    if (new_bw > MAX_BANDWIDTH) {
        new_bw = MAX_BANDWIDTH;
    }
    return create_flow_requirement(flow_id, priority, min_bw, new_bw);
}

pub fn count_active_flows(flow_array: u64) -> u32 {
    let;
    count = 0;
    if (get_current_bandwidth(get_flow_req(flow_array, 0)) > 0) {
        count = (count + 1);
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 1)) > 0) {
        count = (count + 1);
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 2)) > 0) {
        count = (count + 1);
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 3)) > 0) {
        count = (count + 1);
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 4)) > 0) {
        count = (count + 1);
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 5)) > 0) {
        count = (count + 1);
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 6)) > 0) {
        count = (count + 1);
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 7)) > 0) {
        count = (count + 1);
    }
    return count;
}

pub fn find_reclaimable_bandwidth(state: u32, flow_array: u64) -> u32 {
    let;
    let;
    if (get_current_bandwidth(get_flow_req(flow_array, 0)) > 0) {
        total_used = (total_used + get_current_bandwidth(get_flow_req(flow_array, 0)));
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 1)) > 0) {
        total_used = (total_used + get_current_bandwidth(get_flow_req(flow_array, 1)));
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 2)) > 0) {
        total_used = (total_used + get_current_bandwidth(get_flow_req(flow_array, 2)));
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 3)) > 0) {
        total_used = (total_used + get_current_bandwidth(get_flow_req(flow_array, 3)));
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 4)) > 0) {
        total_used = (total_used + get_current_bandwidth(get_flow_req(flow_array, 4)));
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 5)) > 0) {
        total_used = (total_used + get_current_bandwidth(get_flow_req(flow_array, 5)));
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 6)) > 0) {
        total_used = (total_used + get_current_bandwidth(get_flow_req(flow_array, 6)));
    }
    if (get_current_bandwidth(get_flow_req(flow_array, 7)) > 0) {
        total_used = (total_used + get_current_bandwidth(get_flow_req(flow_array, 7)));
    }
    if (allocated > total_used) {
        return (allocated - total_used);
    }
    return 0;
}

pub fn prioritize_bandwidth(flow_array: u64, available_bw: u32) -> u64 {
    let;
    remaining_bw = available_bw;
    let;
    f0 = get_flow_req(flow_array, 0);
    let;
    f1 = get_flow_req(flow_array, 1);
    let;
    f2 = get_flow_req(flow_array, 2);
    let;
    f3 = get_flow_req(flow_array, 3);
    let;
    f4 = get_flow_req(flow_array, 4);
    let;
    f5 = get_flow_req(flow_array, 5);
    let;
    f6 = get_flow_req(flow_array, 6);
    let;
    f7 = get_flow_req(flow_array, 7);
    return create_flow_array(update_flow_bandwidth(f0, calculate_fair_share(remaining_bw, 8)), update_flow_bandwidth(f1, calculate_fair_share(remaining_bw, 8)), update_flow_bandwidth(f2, calculate_fair_share(remaining_bw, 8)), update_flow_bandwidth(f3, calculate_fair_share(remaining_bw, 8)), update_flow_bandwidth(f4, calculate_fair_share(remaining_bw, 8)), update_flow_bandwidth(f5, calculate_fair_share(remaining_bw, 8)), update_flow_bandwidth(f6, calculate_fair_share(remaining_bw, 8)), update_flow_bandwidth(f7, calculate_fair_share(remaining_bw, 8)));
}

