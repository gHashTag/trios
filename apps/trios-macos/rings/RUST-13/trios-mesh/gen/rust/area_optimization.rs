// Generated from .t27 spec
// DO NOT EDIT — generated by t27c

pub const OPT_NONE: u32 = 0;

pub const OPT_RESOURCE_SHARE: u32 = 1;

pub const OPT_BIT_WIDTH_REDUCE: u32 = 2;

pub const OPT_CONST_FOLD: u32 = 3;

pub const OPT_FIFO_TO_RAM: u32 = 4;

pub fn estimate_complexity(func_count: u32, state_count: u32, max_width: u32) -> u32 {
    return (((func_count * 10) + (state_count * 20)) + (max_width * 5));
}

pub fn calculate_sharing_savings(original_count: u32, share_factor: u32) -> u32 {
    if (share_factor == 0) {
        return 0;
    }
    return (original_count - (original_count / share_factor));
}

pub fn calculate_bitwidth_savings(original_width: u32, reduced_width: u32, instance_count: u32) -> u32 {
    if (original_width <= reduced_width) {
        return 0;
    }
    return (((original_width - reduced_width) * instance_count) / 8);
}

pub fn const_folding_applicable(operation_count: u32, const_operand_ratio: u32) -> bool {
    return (const_operand_ratio > 30);
}

pub fn fifo_to_ram_applicable(depth: u32, width: u32) -> bool {
    return ((depth >= 16) && (width >= 8));
}

pub fn create_resource_type(res_type: u32, amount: u32) -> u32 {
    return (((res_type & 0xF) << 28) | (amount & 0xFFFFFFF));
}

pub fn extract_resource_type(resource: u32) -> u32 {
    return ((resource >> 28) & 0xF);
}

pub fn extract_resource_amount(resource: u32) -> u32 {
    return (resource & 0xFFFFFFF);
}

pub fn create_optimization_result(strategy: u32, original: u32, optimized: u32) -> u32 {
    return ((((strategy & 0xF) << 24) | ((original & 0xFF) << 16)) | (optimized & 0xFF));
}

pub fn extract_strategy(result: u32) -> u32 {
    return ((result >> 24) & 0xF);
}

pub fn extract_original(result: u32) -> u32 {
    return ((result >> 16) & 0xFF);
}

pub fn extract_optimized(result: u32) -> u32 {
    return (result & 0xFF);
}

pub fn calculate_savings_percentage(original: u32, optimized: u32) -> u8 {
    if (original == 0) {
        return 0;
    }
    if (original <= optimized) {
        return 0;
    }
    return ();
}

pub fn optimization_worthwhile(result: u32) -> bool {
    return (calculate_savings_percentage(extract_original(result), extract_optimized(result)) > 10);
}

pub fn analyze_bit_width(min_val: u32, max_val: u32) -> u8 {
    if (max_val == 0) {
        return 1;
    }
    if (max_val <= 0xFF) {
        return 8;
    } else {
        if (max_val <= 0xFFF) {
            return 12;
        } else {
            if (max_val <= 0xFFFF) {
                return 16;
            } else {
                if (max_val <= 0xFFFFF) {
                    return 20;
                } else {
                    return 32;
                }
            }
        }
    }
}

