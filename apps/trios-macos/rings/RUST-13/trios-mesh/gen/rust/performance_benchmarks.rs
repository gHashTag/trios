// Generated from .t27 spec
// DO NOT EDIT — generated by t27c

pub const MAX_QUEUE_SIZE: u32 = 16;

pub const MAX_COUNTER: u32 = 255;

pub const TIMER_TICK_US: u32 = 100;

pub fn create_queue(count: u32, head: u32, tail: u32) -> u32 {
    return ((((count & 0xFF) << 16) | ((head & 0xFF) << 8)) | (tail & 0xFF));
}

pub fn queue_count(queue: u32) -> u32 {
    return ((queue >> 16) & 0xFF);
}

pub fn queue_head(queue: u32) -> u32 {
    return ((queue >> 8) & 0xFF);
}

pub fn queue_tail(queue: u32) -> u32 {
    return (queue & 0xFF);
}

pub fn queue_enqueue(queue: u32) -> u32 {
    if (queue_count(queue) < MAX_QUEUE_SIZE) {
        return create_queue((queue_count(queue) + 1), queue_head(queue), ((queue_tail(queue) + 1) % MAX_QUEUE_SIZE));
    } else {
        return queue;
    }
}

pub fn queue_dequeue(queue: u32) -> u32 {
    if (queue_count(queue) > 0) {
        return create_queue((queue_count(queue) - 1), ((queue_head(queue) + 1) % MAX_QUEUE_SIZE), queue_tail(queue));
    } else {
        return queue;
    }
}

pub fn queue_is_full(queue: u32) -> bool {
    return (queue_count(queue) == MAX_QUEUE_SIZE);
}

pub fn queue_is_empty(queue: u32) -> bool {
    return (queue_count(queue) == 0);
}

pub fn inc_counter(counter: u32) -> u32 {
    if (counter < MAX_COUNTER) {
        return (counter + 1);
    } else {
        return 0;
    }
}

pub fn counter_will_overflow(counter: u32) -> bool {
    return (counter == MAX_COUNTER);
}

pub fn ticks_to_microseconds(ticks: u32) -> u32 {
    return (ticks * TIMER_TICK_US);
}

pub fn microseconds_to_ticks(us: u32) -> u32 {
    return (us / TIMER_TICK_US);
}

pub fn ticks_to_milliseconds(ticks: u32) -> u32 {
    return (ticks / 10);
}

