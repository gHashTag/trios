// Generated from .t27 spec
// DO NOT EDIT — generated by t27c

pub const MAX_PATHS: u32 = 4;

pub const MAX_HOPS: u32 = 3;

pub const MIN_PATHS: u32 = 2;

pub const PATH_VALID: u32 = 1;

pub const PATH_INVALID: u32 = 0;

pub fn create_multipath(valid: u32, hop1: u32, hop2: u32, hop3: u32) -> u32 {
    return (((((valid & 0x1) << 24) | ((hop1 & 0xFF) << 16)) | ((hop2 & 0xFF) << 8)) | (hop3 & 0xFF));
}

pub fn get_path_valid(path: u32) -> u32 {
    return ((path >> 24) & 0x1);
}

pub fn get_multipath_hop1(path: u32) -> u32 {
    return ((path >> 16) & 0xFF);
}

pub fn get_multipath_hop2(path: u32) -> u32 {
    return ((path >> 8) & 0xFF);
}

pub fn get_multipath_hop3(path: u32) -> u32 {
    return (path & 0xFF);
}

pub fn create_multipath_state(active: u32, current: u32, flow_id: u32, last_update: u32) -> u32 {
    return (((((active & 0xFF) << 24) | ((current & 0xFF) << 16)) | ((flow_id & 0xFF) << 8)) | (last_update & 0xFF));
}

pub fn get_active_paths(state: u32) -> u32 {
    return ((state >> 24) & 0xFF);
}

pub fn get_current_path(state: u32) -> u32 {
    return ((state >> 16) & 0xFF);
}

pub fn get_flow_id(state: u32) -> u32 {
    return ((state >> 8) & 0xFF);
}

pub fn get_multipath_last_update(state: u32) -> u32 {
    return (state & 0xFF);
}

pub fn create_path_array(p0: u32, p1: u32, p2: u32, p3: u32) -> u64 {
    return ((((() << 48) | (() << 32)) | (() << 16)) | ());
}

pub fn get_multipath(array: u64, index: u32) -> u32 {
    if (index == 0) {
        return ();
    }
    if (index == 1) {
        return ();
    }
    if (index == 2) {
        return ();
    }
    return ();
}

pub fn count_valid_paths(path_array: u64) -> u32 {
    let;
    count = 0;
    if (get_path_valid(get_multipath(path_array, 0)) == PATH_VALID) {
        count = (count + 1);
    }
    if (get_path_valid(get_multipath(path_array, 1)) == PATH_VALID) {
        count = (count + 1);
    }
    if (get_path_valid(get_multipath(path_array, 2)) == PATH_VALID) {
        count = (count + 1);
    }
    if (get_path_valid(get_multipath(path_array, 3)) == PATH_VALID) {
        count = (count + 1);
    }
    return count;
}

pub fn is_multipath_viable(path_array: u64) -> u32 {
    return (count_valid_paths(path_array) >= MIN_PATHS);
}

pub fn select_primary_path(path_array: u64, quality_array: u64) -> u32 {
    if !(is_multipath_viable(path_array)) {
        return 0xFF;
    }
    if (get_path_valid(get_multipath(path_array, 0)) == PATH_VALID) {
        return 0;
    } else {
        if (get_path_valid(get_multipath(path_array, 1)) == PATH_VALID) {
            return 1;
        } else {
            if (get_path_valid(get_multipath(path_array, 2)) == PATH_VALID) {
                return 2;
            } else {
                return 3;
            }
        }
    }
}

pub fn calculate_path_diversity(path_array: u64) -> u32 {
    let;
    let;
    if (get_path_valid(get_multipath(path_array, 0)) == PATH_VALID) {
        hop1_set = (hop1_set | (1 << get_multipath_hop1(get_multipath(path_array, 0))));
    }
    if (get_path_valid(get_multipath(path_array, 1)) == PATH_VALID) {
        hop1_set = (hop1_set | (1 << get_multipath_hop1(get_multipath(path_array, 1))));
    }
    if (get_path_valid(get_multipath(path_array, 2)) == PATH_VALID) {
        hop1_set = (hop1_set | (1 << get_multipath_hop1(get_multipath(path_array, 2))));
    }
    if ((get_path_valid(get_multipath(path_array, 3)) == path_valid) == PATH_VALID) {
        hop1_set = (hop1_set | (1 << get_multipath_hop1(get_multipath(path_array, 3))));
    }
    let;
    count = 0;
    if ((hop1_set & 0x01) == 0x01) {
        count = (count + 1);
    }
    if ((hop1_set & 0x02) == 0x02) {
        count = (count + 1);
    }
    if ((hop1_set & 0x04) == 0x04) {
        count = (count + 1);
    }
    if ((hop1_set & 0x08) == 0x08) {
        count = (count + 1);
    }
    if ((hop1_set & 0x10) == 0x10) {
        count = (count + 1);
    }
    if ((hop1_set & 0x20) == 0x20) {
        count = (count + 1);
    }
    if ((hop1_set & 0x40) == 0x40) {
        count = (count + 1);
    }
    if ((hop1_set & 0x80) == 0x80) {
        count = (count + 1);
    }
    return count;
}

pub fn distribute_load(path_array: u64, current_path: u32, load_ratio: u32) -> u32 {
    let;
    total_paths = count_valid_paths(path_array);
    if (total_paths < 2) {
        return current_path;
    }
    let;
    let;
    let;
    while ((found == 0) && (attempts < 4)) {
        if (get_path_valid(get_multipath(path_array, next_path)) == PATH_VALID) {
            found = 1;
        } else {
            next_path = ((next_path + 1) % 4);
            attempts = (attempts + 1);
        }
    }
    if (found == 1) {
        return next_path;
    }
    return current_path;
}

pub fn needs_failover(path_array: u64, current_path: u32) -> bool {
    if (current_path >= 4) {
        return false;
    }
    return (get_path_valid(get_multipath(path_array, current_path)) == PATH_INVALID);
}

pub fn perform_failover(state: u32, path_array: u64, failed_path: u32) -> u32 {
    let;
    let;
    let;
    if needs_failover(path_array, current) {
        let;
        backup = distribute_load(path_array, current, 0);
        if ((backup != current) && (backup != 0xFF)) {
            return create_multipath_state(active, backup, flow, 0);
        }
    }
    return state;
}

pub fn calculate_multipath_gain(path_array: u64) -> u32 {
    let;
    if (valid_paths >= 2) {
        return (valid_paths * 30);
    }
    return 0;
}

