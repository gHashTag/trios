// Generated from .t27 spec
// DO NOT EDIT — generated by t27c

pub const MAX_CHECKS: u32 = 8;

pub const HEALTH_CRITICAL: u32 = 0;

pub const HEALTH_WARNING: u32 = 1;

pub const HEALTH_HEALTHY: u32 = 2;

pub const CHECK_INTERVAL: u32 = 1000;

pub fn create_health_check(check_type: u32, result: u32, value: u32, timestamp: u32) -> u32 {
    return (((((check_type & 0xF) << 28) | ((result & 0x3) << 26)) | ((value & 0xFF) << 8)) | (timestamp & 0xFF));
}

pub fn get_check_type(check: u32) -> u32 {
    return ((check >> 28) & 0xF);
}

pub fn get_check_result(check: u32) -> u32 {
    return ((check >> 26) & 0x3);
}

pub fn get_check_value(check: u32) -> u32 {
    return ((check >> 8) & 0xFF);
}

pub fn get_check_timestamp(check: u32) -> u32 {
    return (check & 0xFF);
}

pub const CHECK_CPU: u32 = 0;

pub const CHECK_MEMORY: u32 = 1;

pub const CHECK_DISK: u32 = 2;

pub const CHECK_NETWORK: u32 = 3;

pub const CHECK_TEMPERATURE: u32 = 4;

pub const CHECK_POWER: u32 = 5;

pub const CHECK_CONNECTIVITY: u32 = 6;

pub const CHECK_PROCESS: u32 = 7;

pub const RESULT_PASS: u32 = 0;

pub const RESULT_WARN: u32 = 1;

pub const RESULT_FAIL: u32 = 2;

pub const RESULT_SKIP: u32 = 3;

pub fn create_health_array(c0: u32, c1: u32, c2: u32, c3: u32, c4: u32, c5: u32, c6: u32, c7: u32) -> u64 {
    return ((((((((() << 56) | (() << 48)) | (() << 40)) | (() << 32)) | (() << 24)) | (() << 16)) | (() << 8)) | ());
}

pub fn get_health_check(array: u64, index: u32) -> u32 {
    if (index == 0) {
        return ();
    }
    if (index == 1) {
        return ();
    }
    if (index == 2) {
        return ();
    }
    if (index == 3) {
        return ();
    }
    if (index == 4) {
        return ();
    }
    if (index == 5) {
        return ();
    }
    if (index == 6) {
        return ();
    }
    return ();
}

pub fn update_health_check(array: u64, index: u32, new_check: u32) -> u64 {
    if (index == 0) {
        return ((array & 0x00FFFFFFFFFFFFFF) | (() << 56));
    } else {
        if (index == 1) {
            return ((array & 0xFF00FFFFFFFFFFFF) | (() << 48));
        } else {
            if (index == 2) {
                return ((array & 0xFFFF00FFFFFFFFFF) | (() << 40));
            } else {
                if (index == 3) {
                    return ((array & 0xFFFFFF00FFFFFFFF) | (() << 32));
                } else {
                    if (index == 4) {
                        return ((array & 0xFFFFFFFF00FFFFFF) | (() << 24));
                    } else {
                        if (index == 5) {
                            return ((array & 0xFFFFFFFFFF00FFFF) | (() << 16));
                        } else {
                            if (index == 6) {
                                return ((array & 0xFFFFFFFFFFFF00FF) | (() << 8));
                            } else {
                                return ((array & 0xFFFFFFFFFFFFFF00) | ());
                            }
                        }
                    }
                }
            }
        }
    }
}

pub fn calculate_overall_health(array: u64) -> u32 {
    let;
    let;
    if (get_check_result(get_health_check(array, 0)) == RESULT_FAIL) {
        failed = (failed + 1);
    }
    if (get_check_result(get_health_check(array, 1)) == RESULT_FAIL) {
        failed = (failed + 1);
    }
    if (get_check_result(get_health_check(array, 2)) == RESULT_FAIL) {
        failed = (failed + 1);
    }
    if (get_check_result(get_health_check(array, 3)) == RESULT_FAIL) {
        failed = (failed + 1);
    }
    if (get_check_result(get_health_check(array, 4)) == RESULT_FAIL) {
        failed = (failed + 1);
    }
    if (get_check_result(get_health_check(array, 5)) == RESULT_FAIL) {
        failed = (failed + 1);
    }
    if (get_check_result(get_health_check(array, 6)) == RESULT_FAIL) {
        failed = (failed + 1);
    }
    if (get_check_result(get_health_check(array, 7)) == RESULT_FAIL) {
        failed = (failed + 1);
    }
    if (get_check_result(get_health_check(array, 0)) == RESULT_WARN) {
        warnings = (warnings + 1);
    }
    if (get_check_result(get_health_check(array, 1)) == RESULT_WARN) {
        warnings = (warnings + 1);
    }
    if (get_check_result(get_health_check(array, 2)) == RESULT_WARN) {
        warnings = (warnings + 1);
    }
    if (get_check_result(get_health_check(array, 3)) == RESULT_WARN) {
        warnings = (warnings + 1);
    }
    if (get_check_result(get_health_check(array, 4)) == RESULT_WARN) {
        warnings = (warnings + 1);
    }
    if (get_check_result(get_health_check(array, 5)) == RESULT_WARN) {
        warnings = (warnings + 1);
    }
    if (get_check_result(get_health_check(array, 6)) == RESULT_WARN) {
        warnings = (warnings + 1);
    }
    if (get_check_result(get_health_check(array, 7)) == RESULT_WARN) {
        warnings = (warnings + 1);
    }
    if (failed > 0) {
        return HEALTH_CRITICAL;
    } else {
        if (warnings >= 3) {
            return HEALTH_WARNING;
        } else {
            return HEALTH_HEALTHY;
        }
    }
}

pub fn count_failed_checks(array: u64) -> u32 {
    let;
    count = 0;
    if (get_check_result(get_health_check(array, 0)) == RESULT_FAIL) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 1)) == RESULT_FAIL) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 2)) == RESULT_FAIL) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 3)) == RESULT_FAIL) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 4)) == RESULT_FAIL) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 5)) == RESULT_FAIL) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 6)) == RESULT_FAIL) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 7)) == RESULT_FAIL) {
        count = (count + 1);
    }
    return count;
}

pub fn count_warning_checks(array: u64) -> u32 {
    let;
    count = 0;
    if (get_check_result(get_health_check(array, 0)) == RESULT_WARN) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 1)) == RESULT_WARN) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 2)) == RESULT_WARN) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 3)) == RESULT_WARN) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 4)) == RESULT_WARN) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 5)) == RESULT_WARN) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 6)) == RESULT_WARN) {
        count = (count + 1);
    }
    if (get_check_result(get_health_check(array, 7)) == RESULT_WARN) {
        count = (count + 1);
    }
    return count;
}

pub fn is_check_failing(array: u64, check_type: u32) -> bool {
    if ((check_type == CHECK_CPU) && (get_check_type(get_health_check(array, 0)) == CHECK_CPU)) {
        return (get_check_result(get_health_check(array, 0)) == RESULT_FAIL);
    } else {
        if ((check_type == CHECK_MEMORY) && (get_check_type(get_health_check(array, 1)) == CHECK_MEMORY)) {
            return (get_check_result(get_health_check(array, 1)) == RESULT_FAIL);
        }
    }
    return false;
}

pub fn get_health_percentage(array: u64) -> u32 {
    let;
    total = 0;
    let;
    passing = 0;
    if (get_check_result(get_health_check(array, 0)) != RESULT_FAIL) {
        passing = (passing + 1);
    }
    if (get_check_result(get_health_check(array, 0)) != RESULT_SKIP) {
        total = (total + 1);
    }
    if (get_check_result(get_health_check(array, 1)) != RESULT_FAIL) {
        passing = (passing + 1);
    }
    if (get_check_result(get_health_check(array, 1)) != RESULT_SKIP) {
        total = (total + 1);
    }
    if (get_check_result(get_health_check(array, 2)) != RESULT_FAIL) {
        passing = (passing + 1);
    }
    if (get_check_result(get_health_check(array, 2)) != RESULT_SKIP) {
        total = (total + 1);
    }
    if (get_check_result(get_health_check(array, 3)) != RESULT_FAIL) {
        passing = (passing + 1);
    }
    if (get_check_result(get_health_check(array, 3)) != RESULT_SKIP) {
        total = (total + 1);
    }
    if (get_check_result(get_health_check(array, 4)) != RESULT_FAIL) {
        passing = (passing + 1);
    }
    if (get_check_result(get_health_check(array, 4)) != RESULT_SKIP) {
        total = (total + 1);
    }
    if (get_check_result(get_health_check(array, 5)) != RESULT_FAIL) {
        passing = (passing + 1);
    }
    if (get_check_result(get_health_check(array, 5)) != RESULT_SKIP) {
        total = (total + 1);
    }
    if (get_check_result(get_health_check(array, 6)) != RESULT_FAIL) {
        passing = (passing + 1);
    }
    if (get_check_result(get_health_check(array, 6)) != RESULT_SKIP) {
        total = (total + 1);
    }
    if (get_check_result(get_health_check(array, 7)) != RESULT_FAIL) {
        passing = (passing + 1);
    }
    if (get_check_result(get_health_check(array, 7)) != RESULT_SKIP) {
        total = (total + 1);
    }
    if (total == 0) {
        return 100;
    }
    return ((passing * 100) / total);
}

