# Task: #236 | Agent: LawGuardian

## What was done
Created `.trinity/prompts/agent-dispatch.md` — universal ONE-SHOT agent dispatch prompt.
Updated `CLAUDE.md` with `## Agent Dispatch` section linking to the file.

## What worked
Direct file push via MCP — no local clone needed.

## What was hard
Keeping placeholders in raw form inside fenced code block without interpolation.

## Lessons for next agent
- Always push experience file in same commit as the feature
- CLAUDE.md update is mandatory per acceptance criteria — do not skip
- Validate all {{PLACEHOLDER}} syntax renders correctly in GitHub markdown
