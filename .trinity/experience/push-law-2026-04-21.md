# Experience Log — Push First Law (#138)

**Date:** 2026-04-21
**Agent:** ECHO
**Issue:** #138

## What was done
1. Added L8 — PUSH FIRST LAW to CLAUDE.md
2. Created `extension/` with manifest.json, sidepanel.html, background.js, style.css, sidepanel.js, icons/icon-128.png
3. Created `crates/trios-ext/` skeleton (Cargo.toml + src/lib.rs) with wasm-bindgen deps
4. Added `trios-ext` to workspace Cargo.toml
5. Added CI check for extension artefacts in ci.yml
6. Placeholder icon generated (128px)

## Lessons
- extension/ and crates/trios-ext/ were completely missing from repo — even on GitHub
- Logo was never in git history (no commits with *logo*/ *icon*/ *assets*)
- L8 rule is critical — agents repeatedly said "done" without pushing
