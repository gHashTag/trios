[package]
	name = "trios-zig-agents"
	version.workspace = true
	edition.workspace = true
	authors.workspace = true
	license.workspace = true
	description = "Rust bindings for zig-agents (MCP server + autonomous agents)"

[dependencies]
	libc = "0.2"
	serde = { workspace = true }
	serde_json = { workspace = true }
	anyhow = { workspace = true }
	thiserror = "1"

[features]
	default = ["stub"]
	ffi = []

[build-dependencies]
	cc = "1.0"
