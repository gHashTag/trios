[package]
name = "hr-00"
version = "0.1.0"
edition = "2021"
description = "HR-00 — core types: AppState, ChatRequest, ChatResponse, StatusResponse"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["sync"] }
