[package]
name = "hr-01"
version = "0.1.0"
edition = "2021"
description = "HR-01 — route handlers: POST /api/chat, GET /api/status, GET /health"

[dependencies]
hr-00 = { path = "../HR-00" }
axum = "0.7"
serde_json = "1"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
