# Gate-2 attempt — HybridAttn + JEPA push
# Target: BPB < 1.85 on 3 seeds {43, 44, 45}

[training]
seed = 43  # Override via TRIOS_SEED env for other seeds
steps = 4000  # Minimum for Gate-2 (can extend to 27K if promising)
batch_size = 32
lr = 0.004
checkpoint_interval = 1000
eval_interval = 500
train_path = "data/datasets/fineweb10B_sp4096/fineweb_train_000.bin"
val_path = "data/datasets/fineweb10B_sp4096/fineweb_val_000.bin"

[model]
d_model = 384
n_layers = 4
context_len = 6
ff_mult = 4

[jepa]
mask_ratio = 0.30
ema_decay = 0.996

[ledger]
path = "assertions/seed_results.jsonl"
push_to_repo = false
