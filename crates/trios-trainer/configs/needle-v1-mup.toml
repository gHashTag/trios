# Needle V1 muP-transfer variant
# Uses μP (maximal update parametrization) for transfer learning

[training]
seed = 43
steps = 27000
batch_size = 32
lr = 0.004
checkpoint_interval = 1000
eval_interval = 500

[model]
d_model = 384
n_layers = 4
context_len = 6
ff_mult = 4

[ledger]
path = "../../assertions/seed_results.jsonl"
push_to_repo = false
