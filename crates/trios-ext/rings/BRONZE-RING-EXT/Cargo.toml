[package]
name = "trios-ext-bronze"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "BRONZE-RING-EXT — Chrome Extension output ring (manifest, HTML, CSS, dist artifacts)"

# This is a virtual manifest — no [lib] since this ring produces no Rust library.
# The actual build output goes to dist/ via wasm-pack / xtask.
