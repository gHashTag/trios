[package]
name = "trios-ipc"
version = "0.1.0"
edition = "2021"
description = "trios-ipc — typed IPC contract between trios-ui and trios-ext rings"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
