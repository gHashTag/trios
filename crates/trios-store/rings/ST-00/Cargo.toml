[package]
name = "trios-store-st00"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "ST-00 — Store schema types (rows mirroring the drizzle SQLite schema)"
publish = false

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
