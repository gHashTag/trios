[package]
name = "trios-sacred"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Rust bindings for zig-sacred-geometry (phi-attention, Beal conjecture search)"

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ["cfg(has_zig_lib)"] }

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
