[package]
name = "trios-chat-cr-chat-04"
version = "0.1.0"
edition = "2021"
description = "CR-CHAT-04 — padding: fixed-size padding classes (R-CHAT-9) for Trinity Secure Chat"
publish = false

# Anchor: phi^2 + phi^-2 = 3 · TRINITY · CHAT · ZERO-METADATA
# Silver-tier ring — pure data layout, no async, no I/O, no crypto.
# Sibling rings: CR-CHAT-00 (types), CR-CHAT-01 (sealed) — sealed depends on this.

[dependencies]
trios-chat-cr-chat-00 = { path = "../CR-CHAT-00" }
sha2              = "0.10"
subtle            = "2.5"
