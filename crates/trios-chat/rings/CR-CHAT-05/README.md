# CR-CHAT-05 — Persistence (Silver-tier trait + in-memory store)

Sync `Store` trait + `MemoryStore` reference impl. Async SeaORM impl
lives in sibling `BR-IO-CHAT-05`. See `RING.md` for the contract.

🌻 `φ² + φ⁻² = 3`
