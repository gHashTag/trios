[package]
name = "trios-chat-cr-chat-05"
version = "0.1.0"
edition = "2021"
description = "CR-CHAT-05 — chat-persist: Silver-tier persistence trait + in-memory reference store. Concrete SeaORM-Postgres impl lives in sibling BR-IO-CHAT-05."
publish = false

# Anchor: phi^2 + phi^-2 = 3 · TRINITY · CHAT · ZERO-METADATA
# Silver-tier ring — pure logic + sync trait, no I/O, no async.

[dependencies]
trios-chat-cr-chat-00 = { path = "../CR-CHAT-00" }
serde      = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
