[package]
name = "trios-chat-cr-chat-00"
version = "0.1.0"
edition = "2021"
description = "CR-CHAT-00 — chat-types: wire-format primitives for Trinity Secure Chat (EnvelopeMeta, SessionId, Counter, Error, Result, R-CHAT-1..12 law table)"
publish = false

# Anchor: phi^2 + phi^-2 = 3 · TRINITY · CHAT · ZERO-METADATA
# Silver-tier ring — pure data + serde, no I/O, no async, no crypto.
# All other CR-CHAT-* and BR-IO-CHAT-* rings depend on this one.

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
