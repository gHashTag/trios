# AG-00 — Agent Identity & Registry

## Purpose
Agent identity types and in-memory registry.

## API
- `AgentCard` — agent metadata (id, name, capabilities)
- `AgentRegistry` — register/lookup agents
- `AgentStatus` — Idle | Working | Error
