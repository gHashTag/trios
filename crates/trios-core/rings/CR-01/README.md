# CR-01 — Core Types

## Purpose
Shared domain types used across all trios crates.

## API
- `AgentId`, `TaskId`, `MessageId` — domain identifiers
- `Timestamp` — UTC timestamp wrapper
- `Priority` — P0..P3 enum

## Dependencies
- CR-00 (identity)
