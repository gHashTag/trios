# BR-OUTPUT AGENTS

Soul-name: `Loop-Locksmith` · Codename: `DELTA` · Tier: 🥉 Bronze

## Mission

Be the single dispatch point downstream callers reach for: register a `PipelineCfg`, hand over a `TrainerBackend`, get back a `VictoryReport` or `HonestNotYet`. Wire SR-00 types, SR-01 FSM transitions, SR-03 BPB writer, SR-04 gardener into one O(1)-per-chunk loop.

## Honest disclosure

Trainer is mocked (`MockedTrainer`). Real GPU runs land via BR-IO once SR-02 (#454) ships. The INV-7 victory gate (`check_victory`, `stat_strength`, all 18 falsifiers) is byte-equivalent with the legacy `trios-igla-race::victory` — that port is non-negotiable for the gate to remain trustworthy.

## Trailer

`Agent: Loop-Locksmith`
