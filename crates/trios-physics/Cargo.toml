[package]
name = "trios-physics"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Rust bindings for zig-physics (quantum, QCD, gravity simulations)"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
