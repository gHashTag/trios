# RING — trios-igla-race-hack (Gold Crate, GOLD III)

## Identity

| Field    | Value |
|----------|-------|
| Metal    | 🥇 Gold |
| Type     | Crate (nested workspace) |
| Position | GOLD III — outreach + community surface |
| Sealed   | No |

## Purpose

Materialise the partnership-plan vocabulary into typed Rust so every
outreach artefact (DMs, PR comments, leaderboard rows, Discord posts)
speaks one language: **PipelineO1** vs **AlgorithmEntry** vs **Lane**
vs **Gate** vs **RingTier**.

This crate fixes the persistent `pipeline ≠ algorithm` confusion that
leaked into every PR comment, DM, leaderboard row, and Discord message
generated by SR-HACK-01..05.

## Why this crate exists

Without a single typed glossary, every SR-HACK-* ring re-invents the
same 5–10 strings, drifts apart, and silently breaks the partnership
plan's L0 contract. By pinning vocabulary in `Term`, `Lane`, `Gate`,
`RingTier`, the entire GOLD III branch shares one truth.

## Ring Structure (L-ARCH-001)

```
crates/trios-igla-race-hack/
├── src/lib.rs                     ← re-export facade (≤ 50 LoC)
├── Cargo.toml                     ← outer GOLD, nested workspace
├── RING.md                        ← this file
└── rings/
    └── SR-HACK-00/                ← the only ring on day 1
        ├── README.md
        ├── TASK.md
        ├── AGENTS.md
        ├── RING.md
        ├── Cargo.toml
        └── src/lib.rs
```

Future rings (SR-HACK-01..05) plug in here. SR-HACK-00 stays the
bottom of the dependency graph forever.

## Laws

- L1 — no `.sh` files
- L3 — `cargo clippy --all-targets -- -D warnings` clean
- L4 — tests before merge
- L13 — I-SCOPE: only this crate
- L14 — `Agent: <CODENAME>` trailer on every commit
- R-RING-FACADE-001 — `src/lib.rs` is re-exports only

## Anchor

`φ² + φ⁻² = 3` · TRINITY · GOLD III
