# Trinity Mesh Node — Railway config
# φ² + φ⁻² = 3
[build]
builder = "DOCKERFILE"
dockerfilePath = "crates/trios-mesh-node/Dockerfile"

[deploy]
restartPolicyType = "ALWAYS"
healthcheckPath = "/health"
healthcheckTimeout = 30
